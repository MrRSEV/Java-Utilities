plugins {
    id 'java-library'
}

group = 'de.rsev.utilities'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    // no external dependencies by design
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
